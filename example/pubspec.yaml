name: tagaddod_shaker_example
description: Example app for tagaddod_shaker package.
publish_to: "none"

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  get_it: ^8.2.0
  shared_preferences: ^2.0.15
  tagaddod_shaker:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
